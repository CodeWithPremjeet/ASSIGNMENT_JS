// 18. Display Every Element Using forEach()
// Create an array containing five colors and use forEach() to display every color.

let colors = ["Red", "Blue", "Green", "White", "Pink"];
colors.forEach(element => {
    console.log(element);
})